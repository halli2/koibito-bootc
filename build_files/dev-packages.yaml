---
# Packages to install
packages:
  # copr:
  #   - user/repo

  install:
    - rustup
    # Rust dev deps
    - clang # mold dep
    - mold # Soft
    - npm
    - openssl-devel

    # Env variable management
    - direnv
